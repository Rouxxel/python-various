# Logo components

`Logo.tsx` exports a Tailwind-based React `Logo` component with `className`, `size`, `label`, `iconSrc`, and `iconAlt` props.

It currently depends on a `cn` helper at `@/lib/utils`; replace that import with your project’s class-name utility or concatenate classes directly when copying it into another project.
