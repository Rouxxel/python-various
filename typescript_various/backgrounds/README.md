# Background components

`FrutigerAeroBackground.tsx` exports `AeroBackground`, a Tailwind-based React ambient background with optional `imageSrc` (default: `/hero-background.jpg`). Place it near the application root; it is fixed and rendered behind content.

It expects Tailwind utility classes. Copy or adapt its inline gradients and `Bubble` helper when Tailwind is not available.
