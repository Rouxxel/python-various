# Copy to .env for Docker Compose or local tooling. Do not commit real secrets.
ASPNETCORE_ENVIRONMENT=Development
HOST_PORT=8080
API_TITLE=C# REST API Template
API_VERSION=1.0.0
API_DESCRIPTION=A template for building REST APIs with ASP.NET Core

# Optional: only required when an application feature explicitly uses RSA keys.
# E_PUBLIC_KEY=
# E_PRIVATE_KEY=
